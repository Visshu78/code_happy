port: 7880
bind_addresses:
    - ""  # Empty quotes means 0.0.0.0 (All IPv4 and IPv6)
rtc:
    udp_port: 7882
    tcp_port: 7881
    use_external_ip: true # Important for mobile connections
keys:
    # API Key : API Secret (Keep these simple for dev)
    devkey: secret
logging:
    json: false
    level: info